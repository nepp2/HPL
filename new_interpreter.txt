
let view = create_sdl_view(400, 400)

let s = ""

while true {
  let e = poll_event(view)
  if e != () {
    if e.tag == "Quit" {
      break
    }
    if len(e.attribs) > 0 {
      print(e.attribs)
    }
  }
}
