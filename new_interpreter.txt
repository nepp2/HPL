
let view = create_sdl_view(400, 400)

let s = ""

while true {
  let e = poll_event(view)
  if e != () {
    let t = type_name(e)
    if t == "sdl_event_quit" {
      break
    }
    if t == "sdl_event_keydown" {
      if e.key == "Escape" {
        break
      }
      print(e.key)
    }
  }
}
