
struct range
  start : i64,
  limit : i64,
end

struct string
  data : ptr(u8)
  length : u64
end

cfun malloc(size: u64) : ptr(u8)

struct lib_handle
  handle : u64
end

cfun load_library(name : ptr(string)) : lib_handle

cfun load_symbol(handle : lib_handle, name : ptr(string)) : ptr(u8)

cfun print(s : ptr(string))

fun max(a : i64, b : i64)
  if a > b then a else b end
end

fun id()
end

cfun hello()
