
struct range
  start : i64,
  limit : i64,
end

struct string
  data : ptr(u8)
  length : u64
end

cfun malloc(size: u64) : ptr(u8)

struct lib_handle
  handle : u64
end

cfun load_library(name : string) : lib_handle

cfun hello()
